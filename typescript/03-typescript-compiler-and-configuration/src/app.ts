import fs from 'node:fs';

// fs.readFileSync()

let username: string;

username = 'Rohitash Kator';

console.log(username);

function add(a: number, b: number): number {
  return a + b;
}

add(4, 5);
